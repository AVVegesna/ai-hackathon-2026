# Detection model dependencies for PythonScript/detector.py
#
# IMPORTANT: needs Python 3.11 or 3.12. Python 3.13+ has no published
# torch/ultralytics wheels, so the install fails there.
#
# See SETUP.md in the repository root for the full setup steps.

# Video decode/encode and array maths
opencv-python>=4.9
numpy>=1.26
imageio>=2.34
imageio-ffmpeg>=0.5

# YOLO / YOLO-World inference
ultralytics>=8.3
torch>=2.2
torchvision>=0.17

# YOLO-World text encoder. Not on PyPI — installed from the Ultralytics fork.
clip @ git+https://github.com/ultralytics/CLIP.git
